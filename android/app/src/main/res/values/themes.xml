<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light appearance. The dark counterpart lives in values-night/themes.xml
         so system-drawn widgets (the approval-style Switch) follow the phone the
         way the hand-drawn palette in Theme.kt does. Window and bar colours are
         set in code from that palette. -->
    <style name="AppTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:colorAccent">#315CF5</item>
    </style>

    <style name="ApprovalDialogTheme" parent="android:style/Theme.Material.Light.Dialog.Alert">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:colorAccent">#315CF5</item>
    </style>
</resources>
